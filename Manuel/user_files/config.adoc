[[config, Configuration]]
== Configuration ==

La __configuration__ concerne aussi bient la configuration d'une seule <<song, chanson>> ou de tout un <<songbook, carnet de chant>>.

----
OPTIONS
-------
sensitivity: <value> # degré de sensibilité aux erreurs
              high/haut # Le programme s'arrête dès la première erreur
              low/bas # aucune erreur n'est signalée, corrections en coulisses
mode: strict (aucun changement dans l'ordre des chansons)
labels_in_title_band: true # => on met les lables "Paroles", "Musique"
keep_order: true # => aucun changement dans l'ordre des chanson
shrink_diag: true   # => on peut rétrécir les diagrammes (diags)
             false  # => on ne peut pas rétrécir les diags
shrink_tabla: true # on peut réduire pour que ça tienne
shrink_score: true # idem avec les partitions

----

[[config-file, fichier de configuration]]
=== Fichier de configuration ===

C'est un fichier YAML qui contient tout ce que le programme doit savoir sur votre carnet de chant pour le construire correctement, et garder une homogénéïté entre toutes les chansons, aussi disparates soit-elles.

[[universal-properties, Propriétés universelles]]
=== Propriétés universelles ===

On appelle __propriétés universelles__ les propriétés qui peuvent être attribuées à un grand nombre de paramètres de configuration.

[cols="1,2,4", header]
|====
| Propriété
| Description 
| Valeurs

| `font`
| La fonte générale, par défaut. Une  police et une taille.
| Par défaut : Helvetica 11pt

| `position` 
| Position de l'élément dans la page
| Top = en haut, Left = à gauche, Bot = en bas, Right = à droite, Int = à l'intérieur (des double-pages), Ext = à l'extérieur (des double pages)

| `tabla`
| Tout ce qui concerne les tablatures
|

|====



=== Placement des diags ===

Dans le <<config-file>>, c'est le paramètre principal `diags` qui peut contenir les propriétés suivantes :

----
diags:
  position: Top
  scale: 100
----

=== Tablatures et partitions ===

----
tablas:
  position: Under
  
----

=== Placement du titre et infos ===